<Window x:Class="MoneyFlowWPF.InputCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новая категория" 
        Height="180" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите название категории:" 
                   Grid.Row="0" 
                   Margin="0,0,0,5"/>

        <TextBox x:Name="NameTextBox" 
                 Grid.Row="1" 
                 Height="25" 
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Тип:" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <RadioButton x:Name="IncomeRadio" Content="Доход" GroupName="Type" Margin="0,0,15,0"/>
            <RadioButton x:Name="ExpenseRadio" Content="Расход" GroupName="Type" IsChecked="True"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Создать" Width="80" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>